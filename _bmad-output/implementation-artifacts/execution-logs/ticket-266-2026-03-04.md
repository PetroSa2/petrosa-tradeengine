# Ticket #266 Execution Plan: Fix Dispatcher Logging Error

## Problem Statement
`tradeengine/dispatcher.py` uses `event=` keyword argument with standard `logging.Logger`, causing `TypeError` and aborting risk management logic.

## Execution Strategy
- **Mode**: Balanced
- **Target Component**: `petrosa-tradeengine/tradeengine/dispatcher.py`

## Proposed Changes
1. **Identify and Fix Logger Calls**: Locate all instances where `self.logger.info`, `self.logger.error`, etc., are called with the `event=` keyword argument and remove it or convert it to a standard format.
2. **Verification**:
   - Create a reproduction unit test that triggers the failure.
   - Run the test to confirm the fix.
   - Run the full test suite to ensure no regressions.

## Expected Outcome
- Eliminate `TypeError` in Dispatcher logging.
- Ensure risk management logic completes without interruption.
- Successful passing of unit tests.
