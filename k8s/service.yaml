apiVersion: v1
kind: Service
metadata:
  name: petrosa-tradeengine-service
  namespace: petrosa-apps
  labels:
    app: petrosa-tradeengine
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 8000
    protocol: TCP
    name: http
  selector:
    app: petrosa-tradeengine 